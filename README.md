# PGM Image Viewer

An Android app that selects and opens only PGM files from local storage. If a non-PGM file is selected, it shows the toast message `Only PGM files are supported.`. After opening a PGM file, the image can be zoomed in and out, but it cannot be rotated or moved.
